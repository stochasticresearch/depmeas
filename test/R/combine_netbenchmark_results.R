## Combine the results from run_netbenchmark_tests.R for CIM and the remaining

rm(list = ls())
cat("\014")

rResultsRepo = "/home/kiran/data/netbenchmark/r_outputs"
matlabResultsRepo = "/home/kiran/data/netbenchmark/outputs"

# get data for the CIM runs & the other estimator runs

# combine into one overall dataframe

# convert to matlab and output